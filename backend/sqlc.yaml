version: "2"
sql:
  - engine: "postgresql"
    schema: "internal/db/migration"
    queries: "internal/db/query"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_pointers_for_null_fields: true
